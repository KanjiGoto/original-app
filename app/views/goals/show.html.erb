<div class="contents">
    <nav>left</nav>
    <main>
        <div class="main-box">
        <h3>目標</h3>
        
          <div class="individual-goal">
              
            <p class="important"><%= @goal.goal %></p>
            <p><%= @goal.reason %></p>
            <p><%= @goal.future %></p>
            
            <%= link_to "削除", "/goals/#{@goal.id}", method: :delete %>
            <%= link_to "編集", "/goals/#{@goal.id}/edit" %>
          </div>
        
        
         <a href="/users/<%= current_user.id %>" class="return-index">目標一覧にもどる</a>
       
        
        </div>
    </main>
    <aside>right</aside>
</div>